{
  "name": "distributedsystem",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "prisma:generate:payment": "prisma generate --schema=prisma/payment_db.prisma",
    "prisma:generate:server": "prisma generate --schema=prisma/server_config.prisma",
    "prisma:generate:standard": "prisma generate --schema=prisma/standard_db.prisma",
    "prisma:generate": "npm run prisma:generate:payment && npm run prisma:generate:server && npm run prisma:generate:standard",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.0",
    "pg": "^8.11.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0"
  }
}
